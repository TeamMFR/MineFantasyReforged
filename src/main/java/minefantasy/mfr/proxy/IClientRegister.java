package minefantasy.mfr.proxy;

/*
 * Also needs to
 * call MineFantasyReforged.proxy.addClientRegister(this);
 * in the item constructor!
 * */
public interface IClientRegister {
	void registerClient();
}
